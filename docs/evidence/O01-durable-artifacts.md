# O01 durable artifact evidence

## D01-main integration and PR review corrections

O01 was rebased onto merged D01 main `f45010817edf1db1db5429d05618dcacdc1d3f86`. The `SourceBatch` merge retains D01 scoped coverage/count metadata alongside O01 required artifact references, and the T01 runner retains both `d01-scoped` and `o01-artifacts` scenarios. A later Codex review identified two more retry faults: pipeline paths produced under `INGESTION_DATA_DIR=./data` could be prefixed with `data` twice, and a lost local `.pending` directory or part could leave an unrecoverable multipart checkpoint. Relative artifact paths now have one CWD-relative meaning and are still confined to the configured staging root; a regression includes both possible `data/raw` and `data/data/raw` files. The local adapter classifies only missing checkpoint directories/parts as `artifact_checkpoint`, allowing the manifest to clear upload ID and parts for retry. Other storage I/O failures remain `artifact_unavailable`.

The first D01 integration run exposed another O01/D01 interaction: O01's typed public source-health response omitted D01's staged `coverage`, `latest_attempt`, and last-success fields. Those fields now have bounded typed representations that omit private locators and signed queries. The public response also preserves the sparse active-record count map, so a failed staged attempt does not appear to change the last published count. A FastAPI route regression checks actual serialization.

On this rebased source, the C03 source-identity scenario passed at `tmp/integration/2026-09-24T07-11-24-238Z-743d69d8/` with result SHA-256 `8e49c1c16e0278d9c0955edfce80eb805c5b38d8d9e273474ee0ef746f964f8d`; the D01 scoped scenario passed at `tmp/integration/2026-09-24T07-19-56-232Z-f35280e5/` with result SHA-256 `b699a724b4c501e84ea331d9be4f1aa43cd9483139b681e124d26d8362a13fa5`. Both manifests report `outcome: passed` and `cleanup_result: removed`. The O01 Garage/copied-restore scenario passed at `tmp/integration/2026-09-24T07-21-25-657Z-a8c3c304/` with all 18 manifest assertions true, real vanished-multipart recovery, and copied database/object-byte restore. It records result SHA-256 `be7c615967d9a1bc9cc860a3f2512cfaf21a9759db708605a3a15f46399725aa`, Garage proof SHA-256 `8a89abe3723ef14c259eaaf1763a00c897343087f795d2af899bec48f34052f6`, and source manifest SHA-256 `a81563749fe86e7276c2bb5dc0b39cd78706bd759da5cb7bce56ff56a9ddbedb`. Its manifest reports `outcome: passed`, `cleanup_result: removed`; an independent exact-project label query found zero remaining containers and volumes for `urbanization_t01_e2c5cb54b760`.

After the final source changes, Ruff passed for `app tests`, strict mypy passed for 57 application/test source files, and the full API suite passed **259 tests** with two upstream deprecation warnings in disposable no-network containers. The web typecheck, six Vitest tests, and production build also passed offline in an existing browser image whose `package-lock.json` SHA-256 exactly matched this worktree (`62edd984fefcddf103ea38c80ee9d74ac99fc856d52231bb6dc6fa4002de1ef1`); Vite reported the pre-existing large map chunk warning. The three real-stack runs above used the rebased worktree while review fixes were still uncommitted. A clean-head scenario rerun and exact-head CI/review are required before merging PR 30.

## Merged-P03 clean-head validation

The isolated `codex/o01-normalized` branch was rebased without conflicts onto merged P03 main `96aaa5b9af9626063db88cfc7e5adacfd0ae7e6c`. On O01 code head `f11608e`, Ruff passed for `app tests`; strict mypy passed for 53 application source files and four changed test files; and the full API suite passed **191 tests** in a disposable, no-network container with a read-only worktree mount. All 57 `requirements.lock` package pins matched the installed test image. Two upstream dependency deprecation warnings remain.

The exact-head Garage/PostGIS command below passed at `tmp/integration/2026-09-24T06-22-17-648Z-1e296f1a/`. Its manifest recorded `outcome: passed`, `cleanup_result: removed`, fixture SHA-256 `932eacb03655a3dee1a2838abf9ddf8d5dfa44f744482408611b61a2561bbee2`, 17 true manifest assertions, and no failure. The O01 result SHA-256 was `3634d3e5084c454054001ef409827d3567eb2206c00dfb9d70f51d1f1521e49a`, Garage proof SHA-256 `7786bd7bb5439150a3d01a50750fa1aacbbb2e005ee2096f32120adb4a36657f`, and source manifest SHA-256 `c0f76f175e678b0c29ae45cf79a27362b4695e404724a5f719eaebfa876d61af`. The proof covers private-bucket access, wrong credentials, checksum mismatch, interrupted multipart retry, restart without staging, and copied PostgreSQL/object-byte restore. An independent exact-project check found zero residual containers and volumes for `urbanization_t01_81fb8f217a85`. If another dependency merges into main before O01, rebase and rerun the affected gates before merge.

## PR review and CI corrections

Codex review identified two multipart recovery gaps: a deleted upload ID stayed in the manifest after `NoSuchUpload`, and an upload begun remotely could be orphaned if its first database checkpoint failed. The manifest now clears invalid multipart checkpoint fields while retaining the retryable reference; a failed first checkpoint best-effort aborts the unrecorded upload. A local-sink unit test verifies the abort. The Garage scenario now uploads and checkpoints one real multipart part, deletes the remote upload, observes `artifact_checkpoint`, verifies that both persisted checkpoint fields are cleared, and retries the same reference from a new upload ID.

The first PR CI run also exposed an older C03 direct-adapter call that omitted `data_dir`. O01 now accepts that caller and uses its configured ingestion directory while the normal pipeline still passes the explicit directory. The isolated C03 copied-restore scenario then passed at `tmp/integration/2026-09-24T06-42-34-849Z-175bcebe/`, with result SHA-256 `8e49c1c16e0278d9c0955edfce80eb805c5b38d8d9e273474ee0ef746f964f8d`, `outcome: passed`, and cleanup removed.

After these corrections, Ruff passed for `app tests`, strict mypy passed for 53 app files and four changed tests, and the full API suite passed **192 tests** with the same two upstream warnings. The updated O01 Garage/PostGIS run passed at `tmp/integration/2026-09-24T06-44-25-436Z-c68dbbdc/`: 18 true manifest assertions, the real vanished-upload retry, private-bucket and checksum checks, restart without staging, and copied database/object-byte restore. Fixture SHA-256 remained `932eacb03655a3dee1a2838abf9ddf8d5dfa44f744482408611b61a2561bbee2`; O01 result SHA-256 was `be7c615967d9a1bc9cc860a3f2512cfaf21a9759db708605a3a15f46399725aa`, Garage proof SHA-256 `eadd4ba792d12d14437bbd7b35af85dabd91baf1b51126562efc4f711f5ff87d`, and source manifest SHA-256 `ddf593f95915d9b24459d992952a8c7ffa700a3f3462a2d3cfeee2b48897e39e`. Its manifest recorded `outcome: passed`, `cleanup_result: removed`; an independent exact-project check found zero residual containers or volumes for `urbanization_t01_09457ba78c20`.

A later exact-head Codex review found that hosted deployment preflight could pass malformed S3 settings which the sink would reject at runtime. A shared pure validator now checks endpoint scheme/path/query, region, bucket length, and credentials in both preflight and sink construction without creating a boto3 client or probing a network. Seven focused invalid-configuration cases were added. Ruff, strict mypy for the same app/test targets, and the full API suite then passed **199 tests** with two upstream warnings. This configuration-only edit awaits final-head CI, including its Garage scenario, before merge.

O01 adds a private S3-compatible sink, immutable database references, bounded upload leases, and publication checks. The isolated test command is:

```sh
node scripts/run-integration.mjs --suite api --scenario o01-artifacts
```

The scenario uses the T01 project name, labels, exact-volume cleanup and disposable PostGIS database. Its `o01` Compose profile starts a private Garage 2.3.0 service with no host port. The image is pinned to `dxflrs/garage@sha256:866bd13ed2038ba7e7190e840482bc27234c4afaf77be8cfa439ae088c1e4690`. It uploads synthetic empty, binary and multipart objects, interrupts and resumes one multipart upload, rejects wrong credentials, restarts the API and Garage with empty staging, and audits downloaded hashes. It then copies the disposable database into `o01_restore`, copies object bytes to a second private bucket, verifies each byte stream, registers the restored copy under its distinct sink identity and rechecks the exact required manifest sets. The runner records SHA-256 evidence files under `tmp/integration/<run-id>/` and removes only its own Compose volumes and restore database. It does not touch the working snapshots or external buckets.

**Local validation.** Docker Compose configuration and host/Node 22 runner syntax passed. The pinned image's CLI help confirmed `/garage` startup, `key import`, `layout assign --capacity 1GB`, and bucket grant syntax. Ruff passed for `app tests`, strict mypy passed for `app` and the changed integration/publication tests, and the full installed API suite passed **134 tests** with two dependency deprecation warnings. The first suite attempt mounted only `apps/api` and could not read repository-level `render.yaml`; mounting the complete worktree resolved both fixture failures.

The real-stack command above passed in `tmp/integration/2026-09-24T04-12-03-946Z-70702e12/` against a dirty O01/C03 integration worktree at base commit `1aa8957a4b338eb28f046d93107137bdf2edf601`. The fixture SHA-256 is `932eacb03655a3dee1a2838abf9ddf8d5dfa44f744482408611b61a2561bbee2`; `o01-results.json` SHA-256 is `2e4b2f3c2a13d1381e213d96450dfc509fc884bdebca9fd2cd5c02ad6ebc3d86`; the Garage proof SHA-256 is `2c0001807719f9463b68c99f10dcb08cc6655260d897c4ded06806c46ff3882b`; and the source manifest SHA-256 is `b7906da0b1b79f605f49e70566d42266d969c39d06e81b05ea253ce77c8cf42b`. The runner recorded `outcome: passed`, `cleanup_result: removed`, and no failure. A separate exact-project check found no containers or volumes after cleanup.

The proof verified wrong credentials rejected before and after restart, empty and binary bytes, two 8 MiB multipart objects, an interrupted part resumed after lease expiry, corrupt bytes rejected by checksum, and all reference bytes re-audited after restarting with empty staging. The copied PostgreSQL database and second private Garage bucket preserved all required reference sets with a distinct sink identity. The manifest scenario recorded 14 true assertions, including two concurrent runs sharing a blob, stale lease denial, required-set sealing, a pending source batch with no replay marker, prior agenda revision preserved on pending text, and atomic replacement of four agenda collections after the text became verified. This run is strong behavior evidence, but it is not a final clean-head CI claim; a clean run after dependency integration remains required before merge.

O01 leaves C04 agenda identity/decision retention and P04 environmental activation to their own guides. P04 can use `require_verified_references` under its canonical activation transaction after the shadow import, while retaining the previous active version on failure.

## Review fixes

A required environmental source upload failure now holds dependent Huntsville development merges in the same run. Source health reports `environmental_context_unverified`, while prior canonical records and their verified proximity flags remain unchanged. The isolated PostGIS scenario seeds a flagged subdivision, forces the floodplain upload to fail, checks that no development batch or false zero-context revision is published, then verifies a successful retry updates the record with its flag intact.

Durable raw GeoJSON, PDF, extracted-text and archive writes now reserve against `ARTIFACT_STAGING_MAX_BYTES` before any payload file is written. Writers sharing a staging root serialize the budget scan and atomic replacement. A local payload is removed only after the matching manifest copy is verified and the local checksum and byte count match; cleanup holds that same lock through hash and unlink so a concurrent retry cannot replace the path between them. Unit tests cover the exact cap, over-cap rejection before upload, concurrent writers, pending-state cleanup denial, mismatched-byte cleanup denial, and forced cleanup/replacement interleaving. The full API suite passes **139 tests** with two dependency deprecation warnings; Ruff and strict mypy pass. The first review-fix real-stack run passed at `tmp/integration/2026-09-24T04-38-15-247Z-12a16ba6/` with fixture SHA-256 `932eacb03655a3dee1a2838abf9ddf8d5dfa44f744482408611b61a2561bbee2`, result SHA-256 `3634d3e5084c454054001ef409827d3567eb2206c00dfb9d70f51d1f1521e49a`, Garage proof SHA-256 `6c15a4be4b15c079d8ce9aa10c4ba9e8dc833f590285c87259b95ac528ab9790`, `cleanup_result: removed`, and no residual project containers or volumes. The staging lock refinements were added after that run, so they need the subsequent clean-head run for final evidence.

## Retention and recovery contract

The manifest records immutable run references and content-addressed blobs. There is no automatic deletion of verified objects or manifest rows in O01; operators must retain any object referenced by a published or auditable run. Local staging is a retry cache, not a backup, and may be removed only after the corresponding object copy is verified. This PR does not set a production retention period or enable hosted ingestion.

Restore the PostgreSQL database and the private object collection as one matched recovery set. Configure the restored sink identity, copy or restore each referenced object, then stream and hash each required object against the manifest before allowing new publication. A database-only backup is insufficient: it can preserve references whose bytes no longer exist. The isolated `o01-artifacts` scenario exercises this with a copied database and a second private Garage bucket; it does not constitute a production backup policy or cloud-provider choice.
