name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Validate readiness plan
        run: node scripts/validate-readiness-plan.mjs

      - name: Install API dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.lock
          python -m pip install --no-deps .
        working-directory: apps/api

      - name: Install web dependencies
        run: npm ci

      - name: Lint API
        run: ruff check .
        working-directory: apps/api

      - name: Typecheck API
        run: mypy app
        working-directory: apps/api

      - name: Test API
        run: pytest
        working-directory: apps/api

      - name: Typecheck web
        run: npm --workspace apps/web run typecheck

      - name: Test web
        run: npm --workspace apps/web run test

      - name: Build web
        run: npm --workspace apps/web run build

      - name: Install Playwright browsers
        run: npm --workspace apps/web exec playwright install --with-deps chromium

      - name: Run Playwright smoke
        run: npm --workspace apps/web run e2e

      - name: Run real API and browser smoke
        run: node scripts/run-integration.mjs --suite live

      - name: Upload integration artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integration-artifacts
          path: tmp/integration
          if-no-files-found: ignore

      - name: Run production Playwright smoke
        run: npm --workspace apps/web run e2e:production

      - name: Run real API and browser smoke
        run: node scripts/run-integration.mjs --suite live

      - name: Upload integration artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integration-artifacts
          path: tmp/integration
          if-no-files-found: ignore
