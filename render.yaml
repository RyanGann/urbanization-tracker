services:
  - type: web
    runtime: docker
    name: urbanization-tracker-api
    branch: main
    rootDir: apps/api
    region: ohio
    plan: starter
    autoDeployTrigger: checksPass
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: urbanization-tracker-db
          property: connectionString
      - key: INGESTION_DATA_DIR
        value: /app/data
      - key: CORS_ORIGINS
        value: https://urbanization-tracker-web.onrender.com
      - key: REVIEWER_API_TOKEN
        generateValue: true
      - key: PHASE3_STORE_BACKEND
        value: postgres
      - key: PROCESSED_STORE_BACKEND
        value: postgres
      - key: PUBLIC_BASE_URL
        value: https://urbanization-tracker-web.onrender.com
      - key: ALERT_DELIVERY_ENABLED
        value: "false"
      - key: ALERT_DELIVERY_RATE_LIMIT
        value: "25"
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USE_TLS
        value: "true"

  - type: web
    runtime: static
    name: urbanization-tracker-web
    branch: main
    plan: free
    autoDeployTrigger: checksPass
    buildCommand: npm ci && npm --workspace apps/web run build
    staticPublishPath: apps/web/dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://urbanization-tracker-api.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

  - type: cron
    runtime: docker
    name: urbanization-tracker-huntsville-ingestion
    branch: main
    rootDir: apps/api
    region: ohio
    plan: starter
    autoDeployTrigger: checksPass
    schedule: "17 8 * * *"
    dockerCommand: sh -c "alembic upgrade head && python -m app.ingestion.cli ingest-huntsville --data-dir /app/data"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: urbanization-tracker-db
          property: connectionString
      - key: INGESTION_DATA_DIR
        value: /app/data
      - key: HOSTED_INGESTION_ENABLED
        value: "false"
      - key: PROCESSED_STORE_BACKEND
        value: postgres
      - key: PHASE3_STORE_BACKEND
        value: postgres

  - type: cron
    runtime: docker
    name: urbanization-tracker-agenda-ingestion
    branch: main
    rootDir: apps/api
    region: ohio
    plan: starter
    autoDeployTrigger: checksPass
    schedule: "43 9 * * 1-5"
    dockerCommand: sh -c "alembic upgrade head && python -m app.ingestion.cli ingest-huntsville-agendas --data-dir /app/data --document-limit 3"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: urbanization-tracker-db
          property: connectionString
      - key: INGESTION_DATA_DIR
        value: /app/data
      - key: HOSTED_INGESTION_ENABLED
        value: "false"
      - key: PROCESSED_STORE_BACKEND
        value: postgres
      - key: PHASE3_STORE_BACKEND
        value: postgres

  - type: cron
    runtime: docker
    name: urbanization-tracker-madison-county-ingestion
    branch: main
    rootDir: apps/api
    region: ohio
    plan: starter
    autoDeployTrigger: checksPass
    schedule: "29 11 * * 1"
    dockerCommand: sh -c "alembic upgrade head && python -m app.ingestion.cli ingest-madison-county --data-dir /app/data"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: urbanization-tracker-db
          property: connectionString
      - key: INGESTION_DATA_DIR
        value: /app/data
      - key: HOSTED_INGESTION_ENABLED
        value: "false"
      - key: PROCESSED_STORE_BACKEND
        value: postgres
      - key: PHASE3_STORE_BACKEND
        value: postgres

  - type: cron
    runtime: docker
    name: urbanization-tracker-alert-delivery
    branch: main
    rootDir: apps/api
    region: ohio
    plan: starter
    autoDeployTrigger: checksPass
    schedule: "*/15 * * * *"
    dockerCommand: sh -c "alembic upgrade head && python -m app.ingestion.cli send-alerts"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: urbanization-tracker-db
          property: connectionString
      - key: PROCESSED_STORE_BACKEND
        value: postgres
      - key: PHASE3_STORE_BACKEND
        value: postgres
      - key: PUBLIC_BASE_URL
        value: https://urbanization-tracker-web.onrender.com
      - key: ALERT_DELIVERY_ENABLED
        value: "false"
      - key: ALERT_DELIVERY_RATE_LIMIT
        value: "25"
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USE_TLS
        value: "true"

databases:
  - name: urbanization-tracker-db
    region: ohio
    plan: basic-1gb
    databaseName: urbanization_tracker
    user: urbanization_tracker
    ipAllowList: []
